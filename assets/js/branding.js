/* Sesiona · Branding genérico
   Este módulo NO fija datos personales de ningún titular.
   La configuración del emisor (nombre, NIF, dirección, etc.) la
   gestiona cada persona desde Ajustes o el asistente de primera
   configuración (setupWizard). Los defaults neutros viven en load()
   (assets/js/app.js) y en seed() para los datos de demo.
   La plantilla de la factura vive en assets/js/invoice.js (se carga después). */
(function () {
  render();
})();
