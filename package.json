{
  "name": "sesiona-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sesiona (web estática). package.json solo para tooling de desarrollo/test; la app no necesita build.",
  "scripts": {
    "test": "node tests/smoke.test.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
